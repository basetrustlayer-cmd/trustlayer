{
  "name": "@trustlayer/api-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc -w",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "echo lint api gateway",
    "typecheck": "tsc --noEmit",
    "test": "echo test api gateway"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@trustlayer/database": "workspace:*",
    "fastify": "^5.0.0",
    "stripe": "^22.1.1",
    "zod": "^3.24.0"
  }
}
